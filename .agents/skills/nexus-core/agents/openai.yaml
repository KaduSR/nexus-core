interface:
  display_name: "Nexus Core"
  short_description: "Repo-specific patterns and workflows for nexus-core"
  default_prompt: "Use the nexus-core repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true